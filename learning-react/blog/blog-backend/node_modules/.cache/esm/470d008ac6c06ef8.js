let Koa,Router,api,bodyParser,mongoose,jwtMiddleware;_c6c‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_c6c‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_c6c‍.w("./api",[["default",["api"],function(v){api=v}]]);_c6c‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_c6c‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_c6c‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();







const { PORT, MONGO_URI } = process.env;

mongoose
.connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false })
	.then(() => {
		console.log('Connected to MongoDB');
	})
	.catch(e => {
		_c6c‍.g.console.error(e);
	});

const app = new Koa();
const router = new Router();

router.use('/api', api.routes());

app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
	_c6c‍.g.console.log('Listening to port %d', port);
});
